import { createSlice, PayloadAction } from "@reduxjs/toolkit";

const initialState = {};

export const SLICE_NAMESlice = createSlice({
  name: "SLICE_NAME",
  initialState,
  reducers: {},
  extraReducers: {},
});
export default SLICE_NAMESlice.reducer;
